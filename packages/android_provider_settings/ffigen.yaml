# Run with `dart run ffigen --config ffigen.yaml`.
name: AndroidProviderSettingsBindings
description: |
  Bindings for `src/android_provider_settings.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/android_provider_settings_bindings_generated.dart'
headers:
  entry-points:
    - 'src/android_provider_settings.h'
  include-directives:
    - 'src/android_provider_settings.h'
preamble: |
  // ignore_for_file: always_specify_types
  // ignore_for_file: camel_case_types
  // ignore_for_file: non_constant_identifier_names
comments:
  style: any
  length: full
